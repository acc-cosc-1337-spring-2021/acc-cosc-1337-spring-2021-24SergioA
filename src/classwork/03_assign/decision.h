//header
#include <string>

bool is_even(int testNumber);

// number grade to letter grade
std::string get_letter_grade_using_if (int grade);

// get letter grade using switch
std::string get_letter_grade_using_switch (int grade);
