//header
/*write function prototype for get_total with one int parameter (units) and
a double parameter rate*/

// -- int are whole numbers -- doubles are decimals
int get_total(int units, double rate);
